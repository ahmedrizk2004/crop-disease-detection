name: crop_ai
description: Crop Disease Detection & Yield Prediction System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1
  fl_chart: ^0.68.0
  provider: ^6.1.2
  shared_preferences: ^2.5.5
  cupertino_icons: ^1.0.6
  flutter_spinkit: ^5.2.1
  lottie: ^3.1.0
  intl: ^0.19.0
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
